<view class="container">
  <image src="{{ latest ? disLeftSrc : leftSrc }}" class="icon"></image>
  <text class="title">{{ title }} </text>
  <image src="{{ first ? disLeftSrc : rightSrc }}" class="icon"></image>
</view>
