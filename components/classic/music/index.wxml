<view class="container" hidden="{{hidden}}">
  <image class="classic-img {{playing?'rotation':''}}" src="{{img}}" />
    <image class="player-img" src="{{!playing ? playSrc : pauseSrc}}" bind:tap="onPlay"></image>
    <image class="tag" src="images/music@tag.png"></image>
    <text class="content">{{content}}</text>
</view>